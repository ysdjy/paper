# gello configs (V1)

- `gello_franka.yaml` — a **copy** of the legacy hardware config
  (`projects/gello_franka_teleop/configs/gello_franka.yaml`) kept here as the V1
  reference. The authoritative hardware calibration still lives in the legacy
  project until re-calibrated for V1. High-level V1 teleop settings (safety,
  mock) are in `configs/teleop_gello.yaml` at the project root.
